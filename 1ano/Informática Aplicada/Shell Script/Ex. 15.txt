#!/bin/bash
if test $# -eq 0 # Se nenhum parametro fuck oi digitado em $1,
                         #da a mensagem de echo e fecha o programa 
then
 echo "fala parametro"
 exit
else
 fire =$(((9 * $1 + 100)/5))
 echo "A temperatura em firenheit é $fire F"
fi
